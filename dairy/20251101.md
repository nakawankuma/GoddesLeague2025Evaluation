# 2025/11/01 作業記録

## Issue #11: /srcを/docsに変更

### 作業内容
- プロジェクトのディレクトリ構造を変更
- `/src` ディレクトリを `/docs` に改名

### 変更箇所
1. **ディレクトリ名**
   - `src/` → `docs/` に改名（コメントで`/docs`が正しいと指摘を受けて修正）

2. **README.md**
   - ファイル構成セクションのディレクトリ表記を `/src/` → `/docs/` に変更

### 影響範囲
- すべてのソースファイル（index.html, package.json, server.js等）が`docs/`ディレクトリ配下に移動
- ドキュメント内のディレクトリ参照を更新
- GitHub Pagesで標準的に使用される`/docs`ディレクトリ名に準拠
