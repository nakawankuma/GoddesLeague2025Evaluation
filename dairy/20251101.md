# 2025/11/01 作業記録

## Issue #11: /srcを/docに変更

### 作業内容
- プロジェクトのディレクトリ構造を変更
- `/src` ディレクトリを `/doc` に改名

### 変更箇所
1. **ディレクトリ名**
   - `src/` → `doc/` に改名

2. **README.md**
   - ファイル構成セクションのディレクトリ表記を `/src/` → `/doc/` に変更

### 影響範囲
- すべてのソースファイル（index.html, package.json, server.js等）が`doc/`ディレクトリ配下に移動
- ドキュメント内のディレクトリ参照を更新
